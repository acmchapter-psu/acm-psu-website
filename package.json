{
  "name": "acm-psu-platform",
  "version": "1.0.0",
  "private": true,
  "description": "ACM PSU \u2014 public digital archive, member portal, and club management platform.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "audit:repo": "node scripts/repo-audit.mjs",
    "check": "npm run build && npm run audit:repo && npm run typecheck && npm test",
    "serve": "python3 -m http.server 8000",
    "dev": "node build.mjs --watch & python3 -m http.server 8000",
    "types:pull": "supabase gen types typescript --linked > platform/lib/database.types.ts",
    "db:push": "supabase db push",
    "db:reset": "supabase db reset",
    "functions:deploy": "supabase functions deploy ai-review && supabase functions deploy application-summary && supabase functions deploy profile-bio-format && supabase functions deploy records-export && supabase functions deploy club-records-sheet-sync && supabase functions deploy prospective-member-assistant --no-verify-jwt && supabase functions deploy event-registration-intake --no-verify-jwt && supabase functions deploy event-registration-provision",
    "test:registration": "node tests/event-registration.test.mjs",
    "test:sheets": "node tests/google-sheets-push.test.mjs",
    "test": "npm run test:registration && npm run test:sheets && npm run test:provisioning",
    "test:provisioning": "node tests/registration-provisioning.test.mjs",
    "test:authorization": "supabase db query --linked -f tests/authorization.test.sql"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
